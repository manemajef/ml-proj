# Suggested aproach to treat outliers and capping

Probably use the conservative capping introduced in the EDA section on numeric data. other then that, use a basic  cap-map or somthing for features which are clearly capped in a comfertable region and dont have any business reason to have real outliers. 

